package com.interview.dynamic.LCS;

/*
a : ADXCPY
b : AXY
o/p - true (b is available in a)
*/

public class i_SequencePatternMatching {

    /*length = LCS(a, b);
    if(b.length() == length)
        return true;
    else
        return false;*/

}
