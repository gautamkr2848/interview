package com.interview.math;

public class Sum {

    //max(int, type of a, type of b)

    public void sum(){
        byte a = 10;
        byte b = 20;
        //byte c = a + b;
        byte c = (byte) (a + b);

        //b++   =>  (type of b) (b + 1)
    }
}
